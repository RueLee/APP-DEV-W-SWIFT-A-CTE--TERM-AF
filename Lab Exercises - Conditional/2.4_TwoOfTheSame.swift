//2.4 Two of the same
//You are given three variables a, b and c. Check if at least two variables have the same value.
//If that is true printAt least two variables have the same value otherwise, print All the values are different.

let a = 10
let b = 20
let c = 7

if a == b || a == c || b == c {
	print("At least two variables have the same value")
}
else {
	print("All the values are different.")
}